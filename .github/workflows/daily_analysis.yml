# .github/workflows/daily_analysis.yml
名称: 每日股票与期货分析

启用:
计划:
    - cron: "0 10 * * 1-5"  # 北京时间18:00
 工作流调度:

作业:
分析:
运行于: ubuntu-latest
工作:
分析:
运行于: ubuntu-latest

    # 👇 把 env 放在这里，所有步骤都能用
 环境:
      DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
      WECHAT_WEBHOOK: ${{ secrets.WECHAT_WEBHOOK }}

步骤:
      - 名称: 拉取代码
使用: 操作/结账@v4

      - 名称: 设置Python环境
用途: actions/setup-python@v3
与:
Python版本: "3.9"

      - 名称: 安装依赖
运行: |
          python -m pip install --upgrade pip
使用 pip 安装 akshare、pandas 和 requests

      - name: 执行分析脚本
运行: python main.py
